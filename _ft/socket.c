#include "socket.h"
#include <sys/types.h>
#include <sys/socket.h>

/**
 * Create socket for receiver
 *
 * @param struct addrinfo
 *      
void preareReceiver(struct addrinfo *servinfo, int socketFd)
{
    struct addrinfo *p;
    
    if (p = servinfo; p != NULL; p = p->ai_next)
    {
        
    }
}
